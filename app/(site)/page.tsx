import { Hero } from "@/components/site/Hero";

/**
 * Homepage. Built section by section against CLIENT_BRIEF.md §17
 * (build order step 3) — hero first, then the remaining sections,
 * with client sign-off before the other pages are started.
 */
export default function HomePage() {
  return <Hero />;
}
